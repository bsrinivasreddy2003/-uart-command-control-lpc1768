# -uart-command-control-lpc1768
UART command &amp; control firmware for LPC1768 ARM Cortex-M3. Receives text commands from PC via serial terminal, parses using strcmp, controls GPIO and reads 12-bit ADC. Bidirectional communication with error handling. Embedded C, Keil uVision.
